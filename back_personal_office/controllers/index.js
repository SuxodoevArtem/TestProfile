const auth = require('./auth.controller');
const finance = require('./finance.controller');
const profile = require('./profile.controller');
const tarif = require('./tarif.controller');

module.exports = {
    auth,
    finance,
    profile,
    tarif
}